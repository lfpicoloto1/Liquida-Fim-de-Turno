# Infraestrutura local

- **`docker-compose.postgres.yml`** — apenas Postgres para desenvolvimento.

```bash
docker compose -f infra/docker-compose.postgres.yml up -d
```

Imagens da aplicação ficam em [`../docker/README.md`](../docker/README.md).
